"""
Python solution for Min and Max in Array problem at https://www.geeksforgeeks.org/problems/find-minimum-and-maximum-element-in-an-array4428/1
difficulty: basic
input is a list of integers
goal is to return a list that contains the minimum and maximum elements of the input list
"""
class Solution:
    def get_min_max(self, arr):
        # use built-in Python sort function
        arr.sort()
        # return first (minimum) and last (maximum) elements
        return [arr[0], arr[-1]]
